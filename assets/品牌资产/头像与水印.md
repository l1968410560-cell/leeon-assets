# leeon 头像与水印规范

## 头像

- 首选：`素材库/IP形象/Q版机器人-bottts/leeon-01.svg` ~ `leeon-05.svg`（Q 版机器人，种子可复现）。
- 建议：选 1 个固定种子作为主形象（如 `leeon-01`），各平台头像统一用同一个，形成记忆点。
- 处理：机器人主色改为强调青 `#22D3EE`，背景用近黑 `#05060B`，正方形构图。
- 备用：后续可加"leeon 机器人 + 数据舱"场景化头像。

## 水印

- 文字水印：`leeon` 小写 + 全角竖线 + 栏目名，如 `leeon｜AI 杂谈`。
- 样式：得意黑或 JetBrains Mono 半粗体，暖白 `#E8EAED` 60-70% 透明度，右下角，16-20px。
- 图内水印：统一放右下角，不与内容重叠；封面水印可去掉（平台封面本身有标识）。
- 角标：视频固定角标用 IP 机器人 + 水印文字组合，透明度 70%。

## 品牌名拼写

- 全小写：`leeon`（不用 Leeon / LEON / 里昂）。
- 中文场合：`数据党 leeon`。
- 标语（可复用）：`数据党 leeon · AI 真相拆解机`。
